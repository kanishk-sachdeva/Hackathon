<!DOCTYPE html>
<html>

<head>
  <title>
    Complete your Registration
  </title>
  <link rel="stylesheet" href="../../css/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/dashboard.css">
</head>

<body class="register">

  <div class="container">

    <div style="font-size: 20px;"
      class="topbanner mt-2 ps-2 text-center pe-2 pb-1 pt-1 bg-primary border rounded border-1 border-primary fw-bold text-start text-white">
      Reading Partnership for Parents (RPP) Registration
    </div>

    <% if (success) { %>
      <form method="post" action="/users/register">

        <div class="form-floating mb-1 mt-2">
          <input required type="email" class="form-control" id="email" name="email" placeholder="name@example.com">
          <label for="email">Email address</label>
        </div>

        <div class="form-floating mb-1">
          <input required type="text" class="form-control" id="name" name="name" placeholder=" ">
          <label for="name">Name of student</label>
        </div>

        <div class="form-floating mb-1">
          <input required type="text" class="form-control" id="parentname" name="parentname" placeholder=" ">
          <label for="parentname">Parent's name</label>
        </div>

        <div class="form-floating mb-1">
          <input required type="text" class="form-control" id="relation" name="relation" placeholder=" ">
          <label for="relation">Relation</label>
        </div>


        <div class="form-floating mb-1">
          <select required class="form-select" aria-label="Default select example" name="age">
            <option disabled selected value> -- select an option -- </option>
            <% for (var i=1; i < 13; i++){ %>
              <option value="<%= i %>">
                <%= i %>
              </option>
              <% } %>
          </select>
          <label for="age">Age of Child</label>
        </div>

        <div class="form-floating mb-1">
          <select required class="form-select" aria-label="Default select example" name="ethnicity">
            <option disabled selected value> -- select an option -- </option>
            <% var options=["American Indian or Alaska Native", "Asian" , "Black or African American"
              , "Native Hawaiian or Other Pacific Islander" , "White" ] %>
              <% options.forEach((ele=> { %>
                <option value="<%= ele %>">
                  <%= ele %>
                </option>
                <% })) %>
          </select>
          <label for="ethnicity">Ethnicity</label>
        </div>

        <div class="form-floating mb-1">
          <input required type="text" class="form-control" id="address" name="address" placeholder=" ">
          <label for="address">Address</label>
        </div>

        <div class="form-floating mb-1">
          <input required type="text" class="form-control" id="hearaboutus" name="hearaboutus" placeholder=" ">
          <label for="hearaboutus">Where did you hear about us?</label>
        </div>


        <div class="form-floating mb-3">
          <textarea class="form-control" placeholder="" id="reason" name="reason" style="height: 100px"></textarea>
          <label for="reason">Reason for particpation</label>
        </div>


        <button id="registersubmit" type="submit"
          class="btn btn-primary w-100 pb-1 pt-1 fw-bolder bg-primary text-uppercase">Submit</button>
      </form>
      <% } %>

      <% if (!success) { %>
        <% if (locals.message) { %>
          <div class="messagebox rounded container alert alert-danger" role="alert" style="background-color: rgba(245, 245, 245, 0.774);">
            <h4 class="alert-heading text-uppercase">OOPS!</h4>
            <hr>
            <p class="fs-5 fw-bolder">We already got your application with this email address, we are currently reviewing it.</p>
            <hr>
            <p class="mb-0">If your application was refused before, contact us.</p>
          </div>
        <% } else { %>
          <div class="messagebox rounded container alert alert-success" role="alert" style="background-color: rgba(245, 245, 245, 0.774);">
            <h4 class="alert-heading text-uppercase">Well done!</h4>
            <hr>
            <p class="fs-5 fw-bolder">Aww yeah,we got your application and will keep you updated with status of your application.</p>
            <hr>
            <p class="mb-0">We will send you login credentials after your account is approved.</p>
          </div>
        <% } %>
        
  
        <button type="button" onclick="window.location.href='/login'" class="btn btn-outline-success w-100">Return to HomePage</button>
      <% } %>
  </div>

  <script src="/reload/reload.js"></script>
  <script src="./js/bootstrap.bundle.min.js"></script>
</body>

</html>